// SPDX-License-Identifier: MIT

pragma solidity ^0.8.15;

contract dayFifteen {
    
    function sort(uint256[] memory array, uint256 length) public pure returns(uint256[] memory) {
        uint256 temp;

        for (uint8 i = 0; i < length; i++) 
        {
            temp = array[i];
        }

        return array;
    }
}